# https://docs.github.com/en/actions/quickstart
name: test cfengine-apt module
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - run: apt update && apt install python3-pip && pip3 install cfbs
      - run: ci/docker.sh
